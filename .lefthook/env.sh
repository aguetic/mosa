# Sourced by lefthook before hook commands. Git hooks (especially from GUI
# clients) run with a minimal PATH that does not include common mise locations.
export PATH="/opt/homebrew/bin:$HOME/.local/bin:/usr/local/bin:$PATH"
